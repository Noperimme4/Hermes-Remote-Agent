# Remote Agent Server Requirements
# Core dependencies only - no heavy frameworks

# Async server
aiofiles>=23.0.0
asyncio-throttle>=1.0.0

# Optional: for TLS support (built into stdlib in Python 3.10+)
# cryptography>=41.0.0

# Development / Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Type checking
mypy>=1.5.0
types-aiofiles>=23.0.0

# Linting
ruff>=0.1.0